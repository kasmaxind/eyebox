{
  "name": "eyebox",
  "version": "1.0.0",
  "private": true,
  "description": "Eyebox — free self-hosted YouTube-like video streaming",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix web",
    "seed": "npm run seed --prefix server",
    "dev": "concurrently -n server,web -c teal,orange \"npm run dev --prefix server\" \"npm run dev --prefix web\"",
    "dev:server": "npm run dev --prefix server",
    "dev:web": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "start": "npm run start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
